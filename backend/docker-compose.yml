  services:
    postgres:
      image: postgres:16
      container_name: fitness-postgres
      env_file:
        - .env
      ports:
        - "5432:5432"
      volumes:
        - fitness_pgdata:/var/lib/postgresql/data

  volumes:
    fitness_pgdata:

